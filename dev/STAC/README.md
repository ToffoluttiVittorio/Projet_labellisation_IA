# Génération STAC

## Utilisation

Mettre toutes les images dans un dossier et lancer le script python en précisant le dossier

Les résultats sont : catalog.json, collection.json, un json par image dans le dossier results/results_STAC
